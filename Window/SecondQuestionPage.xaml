<?xml version="1.0" encoding="utf-8"?>
<!--
    Trang SecondQuestionPage.xaml
    - Hiển thị tiêu đề, nội dung câu hỏi và danh sách đáp án cho người dùng lựa chọn.
    - Sử dụng StackPanel để căn giữa nội dung, đảm bảo giao diện đẹp trên nhiều kích thước màn hình.
    - Các thành phần chính:
        + TitleTextBlock: Hiển thị tiêu đề câu hỏi.
        + BodyTextBlock: Hiển thị nội dung chi tiết của câu hỏi.
        + AnswerList: Danh sách các đáp án, mỗi đáp án là một Button.
    - Mỗi Button đáp án sẽ gọi sự kiện AnswerButtonClick khi được nhấn.
-->

<Page
    x:Class="App1.Window.SecondQuestionPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:App1.Window"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid>
        <!--
            StackPanel căn giữa nội dung trang, giới hạn chiều rộng tối đa và tối thiểu.
            Sử dụng Spacing để tạo khoảng cách giữa các thành phần con.
        -->
        <StackPanel Padding="20"
                HorizontalAlignment="Center"
                VerticalAlignment="Center"
                Spacing="10">

            <!--
            TitleTextBlock: Hiển thị tiêu đề của câu hỏi.
            - FontSize: Cỡ chữ lớn.
            - FontWeight: In đậm.
            - Foreground: Màu chữ trắng.
            - Margin: Khoảng cách dưới 10px.
            -->
            <Button x:Name="TitleTextBlock"
                    FontSize="24"
                    FontWeight="Bold"
                    Foreground="White"
                    Background="#AA000000"
                    BorderThickness="2"
                    Padding="10"
                    HorizontalAlignment="Left"
                    Margin="0,0,0,10"
                    IsHitTestVisible="False"/>

            <!--
            BodyTextBlock: Hiển thị nội dung chi tiết của câu hỏi.
            - FontSize: Cỡ chữ vừa.
            - Foreground: Màu chữ xám nhạt.
            - TextWrapping: Tự động xuống dòng.
            -->
            <Button x:Name="BodyButton"
                    Background="#AA000000"
                    BorderThickness="2"
                    Padding="10"
                    HorizontalAlignment="Left"
                    Width="800"
                    IsHitTestVisible="False">
                <TextBlock x:Name="BodyTextBlock"
                           FontSize="24"
                           Foreground="White"
                           TextAlignment="Left"
                           TextWrapping="Wrap"
                           Width="750"
                           TextTrimming="CharacterEllipsis"/>
            </Button>

            <!--
                Danh sách đáp án
                - Sử dụng ItemsControl để hiển thị các đáp án theo chiều ngang.
                - Mỗi đáp án là một Button, binding dữ liệu từ nguồn dữ liệu.
            -->
            <ItemsControl x:Name="AnswerList">
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <StackPanel Orientation="Horizontal"
                                HorizontalAlignment="Center"/>
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>

                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <!--
                            Button đại diện cho một đáp án.
                            - Content: binding thuộc tính Text của đáp án.
                            - Tag: binding thuộc tính IsCorrect (xác định đáp án đúng/sai).
                            - Click: gọi hàm AnswerButtonClick khi nhấn.
                            - Thiết lập giao diện: chiều cao, margin, font, màu nền, bo góc.
                        -->
                        <Button Content="{Binding Text}"
                            Tag="{Binding IsCorrect}"
                            Click="AnswerButtonClick"
                            Height="40"
                            Margin="20 5 20 5"
                            FontSize="16"
                            Background="#1E1E1E"
                            Foreground="White"
                            CornerRadius="8"
                            HorizontalAlignment="Center"/>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </StackPanel>
    </Grid>
</Page>
